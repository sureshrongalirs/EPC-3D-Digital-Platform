# packages/core/assets

- `studio_small_09_1k.hdr` — HDR environment map for image-based lighting (see
  `Viewer.ts`'s `RGBELoader` setup). Sourced from [Poly Haven](https://polyhaven.com/a/studio_small_09),
  licensed [CC0](https://polyhaven.com/license) (public domain, no attribution required).
  1k resolution, ~1.6MB — kept small since it's bundled with every consumer of
  `@plantscope/core`, not lazily fetched. md5: `d5d7eb9d26d341d6aa4d11c1a54fd97c`.
