# Placeholder Dockerfile — Phase 0 scaffold.
# Real multi-stage workspace build (pnpm install --frozen-lockfile, pnpm --filter @plantscope/api build)
# lands with the API implementation. The worker/api containers get no outbound network (see CLAUDE.md).
FROM node:22-alpine
WORKDIR /app
CMD ["node", "--version"]
