server {
    listen 80;

    location / {
        default_type text/plain;
        return 200 'PlantScope nginx placeholder\n';
    }

    location /api/ {
        proxy_pass http://api:3000/;
    }
}
