node_modules/
dist/
dist-ssr/
coverage/
*.log
npm-debug.log*
.DS_Store
.env
.env.*
!.env.example

# Real client sample files — synthetic fixtures for CI live in testdata/ instead.
/testdata/local/

# docker-compose bind-mount data (postgres data dir, converted models)
deploy/data/

# server/api's local dev DATA_DIR default (SQLite file + uploaded models) — see .env.example
server/api/data/

# Cesium's static asset tree, copied from node_modules at dev/build time (large and fully
# reproducible — see apps/demo/scripts/copy-cesium-assets.mjs), unlike the tiny vendored
# DRACO decoder in apps/demo/public/draco/ which stays committed.
apps/demo/public/cesium/
