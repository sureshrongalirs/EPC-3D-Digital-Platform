{
  "name": "@plantscope/demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/copy-cesium-assets.mjs && vite",
    "build": "node scripts/copy-cesium-assets.mjs && vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@plantscope/globe-view": "workspace:*"
  },
  "devDependencies": {
    "cesium": "^1.143.0",
    "vite": "^6.0.5"
  }
}
