# Test data

- `testdata/` (this directory, tracked in git): synthetic, non-proprietary fixtures used by CI
  and unit tests.
- `testdata/local/` (git-ignored, see root `.gitignore`): real client sample files (FBX, `.mdb2`,
  etc.) for manual local testing only. Never commit real client data here or anywhere else in
  the repo.
